# NV-01 — mapa kontraktów przed implementacją

Repozytorium: `luciferprosun/AIOA-spArkHAT`, prywatne. BASE_SHA: `8f77e9ae9ba26e86658c1ddb46ef6a306db56c77`. Numery linii w tej mapie odnoszą się do tego SHA, nie do późniejszego diffu NV-01. To mapa odczytanego kodu, nie deklaracja testów LIVE.

| Obszar | Istniejący kontrakt / konstrukcja / właściciel | Wejście → wyjście, test i luka G1 |
|---|---|---|
| Jeden runtime | `runtime/main.py:141` AgentRuntime; `:144` konstruktor, `:1295` create_runtime; `runtime/cli.py` istniejący entry point. Runtime posiada lazy CPL, NonZero i MemoryPatch. | Dotychczas konstruktor inicjuje MemoryStore i MemoryHatStore, co zapisuje stan. Doctor wymaga inertnego trybu konstrukcji tego samego runtime, bez drugiego silnika. Testy `test_main.py`, `test_cpl_assistant_cli.py`. |
| Core owner | `runtime/core_admission.py:76` OwnerScope, `:88` LocalOwnerAssignment, `:118` CorePrincipal, `:135` CoreAdmission. Przydział pochodzi wyłącznie z zaufanego composition root. | JSON/model nie tworzy principal ani approval. Użyć istniejących czterech współrzędnych scope; manifest tylko deklaruje oczekiwane powiązanie i musi zgadzać się z Core. Testy `test_memory_patch_authority.py:19`. Brak przydziału = niedostępność, nie domyślny owner. |
| MemoryPatch | `runtime/main.py:329` memory_patch_status i `:340` memory_patch_operator_request; `runtime/memory_patch/contract.py:17` MemoryPatchConfig; `runtime/memory_patch/service.py:83` CoreMemoryPatchDependencies, `:224` MemoryPatchService. Runtime właścicielem lazy service, porty pożyczone od Core. | Config + jawnie podane transakcje/evidence/sources/provenance/provider → istniejący native service. Brak portu nie tworzy SQLite ani mocka. `test_memory_patch_discovery.py`, `test_memory_patch_lifecycle.py`, `test_memory_patch_persistence_ports.py`. G1 tylko inspekcja podłączeń, bez SQL, migracji ani commit. |
| Backend i receipt | `runtime/memory_patch/persistence/ports.py:38` StoredRecord, `:102` RepositoryTransaction, `:125` TransactionFactory. CAS rewizji; niejednoznaczny commit = CommitOutcomeUnknown. | Begin wymaga istniejącego TransactionContext/principal. G1 nie rozszerza migracji CockroachDB ani nie potwierdza produkcyjnego HA/ANN. Zachować `test_memory_patch_lifecycle.py` (explicit reconciliation). |
| Evidence / provenance | `runtime/evidence_admission.py` CoreEvidenceAdmission/CoreEvidenceCatalog/CapturedEvidence; `runtime/memory_patch/service.py:253` NativeRetrieval i `:266` CoreLedgerPublication. | Capture i published evidence z Core → scoped retrieval, później provenance. Źródło/revision/wycofanie muszą być sprawdzane przez port evidence, nie przez deklarację modelu. `test_memory_patch_retrieval.py`, `test_memory_patch_evidence_promotion.py`. |
| CPL 1+3+1 | `runtime/main.py:209` lazy critical_loop; `runtime/critical_loop/service.py:69` CriticalPromptLoopService; `:327` _execute; `runtime/providers/exact.py:82` ExactRequest. | Plan → jawne start(plan_hash, nonce) → primary + trzy krytyki + revision. `ExactRequest.validate` wymaga dokładnie `openrouter`; brak cichego fallback. Koszty z `runtime/critical_loop/policy.py`. `test_cpl_generic.py:169`, `:205`, `test_cpl_service.py`. G1 nie wykonuje pięciu płatnych generacji. |
| Restart CPL / trace | `runtime/critical_loop/service.py:93` _recover; `runtime/critical_loop/evidence.py:96` append, `:102` trace_id równe run_id. NonZero zachowuje własne UUIDv7 (`models/identifiers.py:58`). | Niedokończony run → INTERRUPTED, bez replay. Nowy kontekst misji linkuje istniejący trace_id, nie zmienia identyfikatorów historycznych. `test_cpl_assistant_cli.py:38`, `test_cpl_evidence.py`. |
| NonZero / authority | `runtime/main.py:298` nonzero_status, `:311` lazy nonzero_cloudops; `runtime/nonzero_cloudops/contract.py:34` ModuleConfig; `policy.py:31` ModelPlanCandidate, `:116` PlanRemediation. | Model = propozycja; lokalna polityka i jawna decyzja człowieka warunkują działanie. Native portable nie oznacza autoryzacji produkcyjnego AWS. Testy `test_nonzero_core_authority.py`, `test_nonzero_native.py`, `test_nonzero_parity.py`. Nie zmieniać enumów ani starej ścieżki aprobat. |
| HAT / Linux / prawo DE | `runtime/tools/memory_hats.py` istniejący wybór HAT; `runtime/memory_patch/hats.py:41` NativeHatAdmission; `domain_profiles.py:502` DomainProfile i `:516` GERMAN_LAW_FIXTURE_PROFILE; `memory/rhcsa_context.py` istniejący Linux. | HAT wymaga zgodnego scope, registry, wyboru i przydziału Core. Prawo DE to deterministyczna polityka/fikstury, nie certyfikowana porada ani live law pack. `test_routing_boundary.py:157`, `test_memory_patch_retrieval.py`. Nie dodawać alternatywnego HAT store. |
| Czas | `runtime/memory_patch/retrieval/temporal.py:50` TemporalQueryMode CURRENT/AS_OF/FUTURE/UNSPECIFIED, TemporalPolicy i freshness. | Daty, scope, wersja i źródła decydują o dopuszczalności; wysoki wynik feromonu nie zastępuje weryfikacji. Zachować istniejące enumy i source lineage. |
| NVIDIA / NV-00 | Osobne narzędzie `AIOA_Claw_NVIDIA/bin/nvbuild` i lokalny skill nvidia-build-api. To tooling Codexa, nie adapter produktowego CPL. | Świeży doctor: klucz z lokalnego pliku + TLS PASS, bez inferencji. W tym Core brak certyfikowanego adaptera NVIDIA i bound credentials. G1 pokazuje tę lukę jako UNAVAILABLE, nie przemianowuje OpenRouter. |
| DVM / scheduler | `runtime/adaptive_routing/circadian_router.py:19` select_routing_mode: prosta pora dnia → surface/deep. | To inspiracja routingu, nie scheduler. G1 definiuje pojedynczy SchedulerPort kontrolowany przez AgentRuntime/LITE; bez thread/timera/start. Adaptacyjny budżet wymaga G2/G3 i pomiarów. |
| Delta | `runtime/memory_patch/contracts/records.py:1368` CorrectionCandidate, `:1453` CorrectionRequirement; `correction/packets.py:34` RequiredCorrection. | Nie tworzyć konkurencyjnego DeltaCandidate. Cienkie powiązanie istniejącego CorrectionCandidate z trace, applicability, revision i evidence. Kanonicznie identyczna treść nie tworzy delty; audyt zachowuje wynik NO_CHANGE. Nie udawać ogólnego rozpoznawania równoważności semantycznej. |
| Doświadczenia / feromony | `runtime/memory_patch/contracts/records.py:2095` ModelExperienceEvent i `:2160` assert_model_experience_is_advisory; `enums.py:236` ModelExperienceOutcome. | Istniejący event to hint, nigdy dowód/aprobata/uprawnienie. Brak dokładnego kontraktu trail/VERIFIED_REUSE/FAILED_REUSE/snapshot: potrzebne małe nowe DTO referujące istniejący event, bez produkcyjnego store/scoring/uczenia. Tau+ i tau− rozdzielone; CONFLICT/REVOKED nie odzyskują kwalifikacji przez score. |

## Wynik mapowania

G1 rozszerza jeden composition root i istniejącą serializację/owner boundary. CLI zachowuje nazwę `aioa-sparkhat` i dotychczasowe flagi. Nowe polecenia diagnostyczne są bezstanowe. Syntetyczny composition probe musi dotknąć rzeczywistego AgentRuntime i istniejącego MemoryPatch descriptor; samo testowanie słowników nie spełnia bramki.
